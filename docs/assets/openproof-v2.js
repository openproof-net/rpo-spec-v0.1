(()=>{const menu=document.querySelector('.menu'),links=document.querySelector('.links');if(menu)menu.onclick=()=>{links.classList.toggle('open');menu.setAttribute('aria-expanded',links.classList.contains('open'))};const stages=[...document.querySelectorAll('.stage')],tabs=[...document.querySelectorAll('.rail button')];let at=0;window.opShow=(n)=>{at=Math.max(0,Math.min(stages.length-1,n));stages.forEach((s,i)=>s.classList.toggle('active',i===at));tabs.forEach((t,i)=>t.classList.toggle('active',i===at));document.querySelector('[data-prev]').disabled=at===0;const next=document.querySelector('[data-next]');next.textContent=at===stages.length-1?'Return to overview ↺':'Continue →';if(innerWidth<880)document.querySelector('.mainGrid').scrollIntoView({behavior:'smooth'})};tabs.forEach((t,i)=>t.onclick=()=>opShow(i));document.querySelector('[data-prev]').onclick=()=>opShow(at-1);document.querySelector('[data-next]').onclick=()=>opShow(at===stages.length-1?0:at+1);document.querySelectorAll('.pill').forEach(p=>p.onclick=()=>p.classList.toggle('selected'));opShow(0)})();;(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;document.querySelectorAll('canvas.stars').forEach(canvas=>{const host=canvas.parentElement,ctx=canvas.getContext('2d',{alpha:true});let w=0,h=0,dpr=Math.min(devicePixelRatio||1,2),stars=[];const make=()=>({x:Math.random()*w,y:Math.random()*h,r:.3+Math.random()*1.25,a:.18+Math.random()*.62,vx:(Math.random()-.5)*.055,vy:(Math.random()-.5)*.055,t:Math.random()});function resize(){const r=host.getBoundingClientRect();w=r.width;h=r.height;canvas.width=Math.max(1,Math.floor(w*dpr));canvas.height=Math.max(1,Math.floor(h*dpr));canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);stars=Array.from({length:Math.min(260,Math.max(90,Math.floor(w*h/8500)))},make)}function draw(){ctx.clearRect(0,0,w,h);for(const s of stars){if(!reduce){s.x+=s.vx;s.y+=s.vy}if(s.x<0)s.x=w;if(s.x>w)s.x=0;if(s.y<0)s.y=h;if(s.y>h)s.y=0;const c=s.t<.16?'212,175,55':s.t<.25?'56,224,162':s.t<.34?'93,115,255':'255,255,255';ctx.beginPath();ctx.fillStyle='rgba('+c+','+s.a+')';ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}if(!reduce)requestAnimationFrame(draw)}new ResizeObserver(resize).observe(host);resize();draw()})})();